# Add to ~/.tmux.conf: prefix+a pops the board over any pane, any key closes
bind-key a display-popup -E -w 90% -h 85% 'agentdash -a; read -rsn1'

# Recommended for `agentdash -w --notify` desktop notifications through tmux:
set -g allow-passthrough on
